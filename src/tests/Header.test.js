// import { screen } from '@testing-library/react';
// import userEvent from '@testing-library/user-event';
// import Wallet from '../pages/Wallet';
// import { renderWithRouterAndRedux } from './helpers/renderWith';
// import mockData from './mocks/mockData';
// import initialStateMock from './mocks/initialStateMock';
// const Header = require('../components/Header');

// jest.mock('../components/Header');
describe('Testes do componente Header', () => {
  test('Testa se ao entrar na página da carteira, o valor total é 0', () => {
    // Header.sumExpenses.jest.fn().mockReturnValue(5);
    // const mockSomar = jest.spyOn(Header, 'sumExpenses');
    // Header.sumExpenses(1, 2);
    // expect(Header.sumExpenses).toHaveBeenCalled();
    // expect(Header.sumExpenses).toHaveBeenCalledTimes(1);
    // expect(Header.sumExpenses).toHaveBeenCalledWith(1, 2);
    // expect(Header.sumExpenses(1, 2)).toBe(3);
  });
});
